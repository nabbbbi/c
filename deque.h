#ifndef NEWMOD_H_SENTRY
#define NEWMOD_H_SENTRY
struct item *add_first_deque(double x);
struct item *add_last_deque(double x);
void del_first_element(struct item *first);
void del_last_element(struct item *first);

#endif
